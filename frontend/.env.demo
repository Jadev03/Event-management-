# Client / stakeholder demo: frontend-first.
# If backend is running, connected endpoints are used automatically.
# Run: npm run dev:demo
# Sign in with emails + passwords from src/data/users.js
VITE_DEMO_MODE=true
